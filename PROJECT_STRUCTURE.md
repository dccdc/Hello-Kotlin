# 📁 项目结构说明

## 目录结构

```
mouse_recorder/
│
├── main.py              # 主程序 - GUI界面
├── recorder.py          # 录制模块 - 监听和记录鼠标操作
├── player.py            # 回放模块 - 回放录制的操作
├── requirements.txt     # Python 依赖包列表
│
├── README.md            # 完整项目文档
├── QUICKSTART.md        # 快速开始指南
├── PROJECT_STRUCTURE.md # 项目结构说明（本文件）
│
├── run.sh               # Linux/macOS 运行脚本
├── run.bat              # Windows 运行脚本
└── .gitignore           # Git 忽略文件配置
```

## 核心模块说明

### 1. main.py - 主程序（GUI）

**功能**：提供图形用户界面，整合录制和回放功能

**主要类**：
- `MouseRecorderApp`: GUI应用主类

**界面组件**：
- 信息显示区：显示事件数量、录制时长、当前状态
- 录制控制区：开始/停止录制按钮
- 回放控制区：循环选项、速度选择、开始/停止回放按钮
- 文件操作区：保存、加载、清空按钮

### 2. recorder.py - 录制模块

**功能**：监听并记录鼠标的所有操作

**主要类**：
- `MouseRecorder`: 鼠标录制器

**核心方法**：
- `start_recording()`: 开始录制
- `stop_recording()`: 停止录制
- `save_to_file()`: 保存录制到 JSON 文件
- `load_from_file()`: 从文件加载录制

**录制的事件类型**：
- 鼠标移动 (move)
- 鼠标点击 (click) - 支持左键/右键/中键
- 鼠标滚轮 (scroll)

**数据格式**：
```json
[
  {
    "type": "move",
    "x": 100,
    "y": 200,
    "timestamp": 0.123
  },
  {
    "type": "click",
    "x": 150,
    "y": 250,
    "button": "Button.left",
    "pressed": true,
    "timestamp": 0.456
  }
]
```

### 3. player.py - 回放模块

**功能**：精确回放录制的鼠标操作

**主要类**：
- `MousePlayer`: 鼠标回放器

**核心方法**：
- `play()`: 开始回放（支持循环和速度控制）
- `stop()`: 停止回放
- `is_playing_now()`: 检查是否正在播放

**特性**：
- 精确的时间控制（基于时间戳）
- 可调节播放速度（0.5x - 2.0x）
- 支持循环播放
- 多线程异步播放，不阻塞GUI

## 技术架构

### 依赖库
- **pynput**: 跨平台的鼠标/键盘控制库
  - `pynput.mouse.Listener`: 监听鼠标事件
  - `pynput.mouse.Controller`: 控制鼠标操作

- **tkinter**: Python 内置的 GUI 库（无需额外安装）

### 线程模型
- **主线程**: 运行 GUI 界面
- **监听线程**: pynput 的 Listener 在独立线程中运行
- **回放线程**: 播放操作在独立线程中执行，避免阻塞 GUI

### 数据流

```
录制流程:
用户操作 → pynput监听 → 记录事件+时间戳 → 存储到列表 → 可选保存到文件

回放流程:
加载事件列表 → 计算时间间隔 → 按时间回放 → 执行鼠标操作 → 循环（可选）
```

## 扩展建议

如果想要扩展功能，可以考虑：

1. **添加键盘录制**：集成 pynput.keyboard
2. **截图功能**：在关键步骤自动截图
3. **热键控制**：使用全局热键控制录制/回放
4. **脚本编辑**：可视化编辑录制的操作
5. **条件执行**：根据屏幕内容执行不同操作
6. **云同步**：将录制内容同步到云端
7. **宏变量**：支持参数化的操作序列

## 性能考虑

- **鼠标移动事件频率很高**：可能每秒产生数百个事件
- **优化建议**：
  - 可以添加移动事件的采样率限制
  - 合并连续的移动事件
  - 压缩存储格式

## 安全注意事项

⚠️ **重要提醒**：
- 录制文件可能包含敏感操作（如密码输入）
- 不要分享包含敏感信息的录制文件
- 回放前确认操作内容安全
- 某些防作弊系统可能检测到自动化操作

## 兼容性

✅ **测试通过的平台**：
- Windows 10/11
- macOS 10.14+
- Ubuntu 20.04+

⚠️ **已知限制**：
- macOS 需要辅助功能权限
- 某些 Linux 发行版可能需要 X11 支持
- Wayland 环境可能存在兼容性问题
